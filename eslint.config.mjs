// Root config — lints loose files at the repo root. Each workspace package has
// its own `eslint.config.mjs`; see `eslint.config.base.mjs` for why.
export { default } from './eslint.config.base.mjs';
